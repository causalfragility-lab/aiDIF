% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aidif_core.R
\name{fit_aidif}
\alias{fit_aidif}
\title{Fit the AI-DIF model}
\usage{
fit_aidif(
  human_mle,
  ai_mle = NULL,
  alpha = 0.05,
  scale_by = "pooled",
  tol = 1e-07,
  maxit = 100L
)
}
\arguments{
\item{human_mle}{A validated mle list for human-scored data.}

\item{ai_mle}{A validated mle list for AI-scored data, or \code{NULL}.}

\item{alpha}{Significance level.  Default \code{0.05}.}

\item{scale_by}{Denominator for standardising intercept differences:
\code{"pooled"} (default), \code{"ref"}, or \code{"focal"}.}

\item{tol}{IRLS convergence tolerance.  Default \code{1e-7}.}

\item{maxit}{Maximum IRLS iterations.  Default \code{100}.}
}
\value{
An object of class \code{"aidif"}.
}
\description{
The primary estimation function of \pkg{aiDIF}.  Runs the robust DIF
procedure under both human and AI scoring using the built-in IRLS engine
(\code{\link{estimate_robust_scale}}), then tests for Differential AI
Scoring Bias (DASB).
}
\examples{
dat <- simulate_aidif_data(n_items = 6, seed = 1)
mod <- fit_aidif(dat$human, dat$ai)
print(mod)
summary(mod)

}
\seealso{
\code{\link{estimate_robust_scale}},
  \code{\link{scoring_bias_test}}, \code{\link{simulate_aidif_data}}
}
